#include <iostream>
#include <cstdlib>
#include <string>

using namespace std;

int main()
{	
	string main_str = "Pynative c++ Exercises";
	string sub_str = "c++";
	
	size_t found_ind = main_str.find(sub_str);
	
	if(found_ind != string::npos){
		cout << "Substring found: " << found_ind;
	}else {
		cout << "Substring not found!";
	}
	
return 0;
}
